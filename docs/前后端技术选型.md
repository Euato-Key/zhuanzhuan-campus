# 前后端技术选型

## 项目技术栈概览

| 层级 | 技术选择 |
|------|----------|
| 前端 | Vue 3 + TypeScript + Element Plus |
| 后端 | Node.js + Express.js + TypeScript |
| 数据库 | MySQL|
| ORM | Prisma |
| AI | 阿里云百炼（Kimi2.6） |
| 对象存储 | 阿里云OSS |
| 邮箱 | QQ邮箱SMTP |
| WebSocket | Socket.io |

---

## 一、前端技术选型

### 1.1 核心框架
- **Vue 3**：最新稳定版本，使用Composition API + `<script setup>`
- **TypeScript**：strict模式，开启严格类型检查
- **Vite**：开发服务器和构建工具（比webpack快很多）

### 1.2 状态管理
- **Pinia**：Vue 3官方推荐的状态管理库，比Vuex更轻量、TypeScript友好

### 1.3 UI组件库
- **Element Plus**：你已选，Vue 3版本
  - 按需引入（使用 unplugin-vue-components 自动导入）
  - 自定义主题色（适配校园风格）

### 1.4 路由
- **Vue Router 4**：Vue 3官方路由

### 1.5 网络请求
- **Axios**：
  - 封装请求拦截器（自动带token）
  - 封装响应拦截器（统一错误处理）
  - 请求重试、超时设置

### 1.6 WebSocket
- **socket.io-client**：与后端Socket.io配合，支持自动重连

### 1.7 图片/文件上传
- **阿里云OSS直传**：
  - 前端直传OSS的temp目录（获取STS临时凭证）
  - 后端异步移动到正式目录
  - 进度条显示（使用XMLHttpRequest的upload事件）

### 1.8 样式预处理
- **Sass (SCSS)**：CSS预处理器，变量、嵌套、混入等特性，提升样式开发效率

### 1.8 工具库
| 库 | 用途 |
|----|------|
| dayjs | 日期时间处理（通知时间、订单时间、商品过期等） |
| lodash-es | 工具函数（搜索防抖、表单防抖节流等） |

> 注：复制功能使用原生 `navigator.clipboard.writeText` 即可，无需额外库

### 1.9 表单验证
- **async-validator**：Element Plus内置的验证库，可配合自定义规则

### 1.10 其他辅助
- **eslint + prettier**：代码规范
- **husky + lint-staged**：git提交时自动检查
- **pinia-plugin-persistedstate**：Pinia数据持久化（搜索历史等）

### 1.11 前端目录结构建议

```
frontend/
├── public/                    # 静态资源
├── src/
│   ├── api/                   # API接口封装
│   │   ├── modules/           # 按模块划分（user、product、order...）
│   │   └── index.ts           # axios实例
│   ├── assets/                # 静态资源（图片、字体）
│   │   ├── images/
│   │   └── styles/
│   ├── components/            # 公共组件
│   │   ├── common/            # 通用组件（EmptyState、Loading...）
│   │   └── business/          # 业务组件（ProductCard、OrderCard...）
│   ├── composables/           # 组合式函数
│   │   ├── useAuth.ts         # 认证相关
│   │   ├── useOSS.ts          # OSS上传
│   │   └── useWebSocket.ts    # WebSocket
│   ├── layouts/               # 布局组件
│   ├── router/                # 路由配置
│   │   └── index.ts
│   ├── stores/                # Pinia stores
│   │   ├── user.ts
│   │   ├── cart.ts
│   │   └── ...
│   ├── types/                 # TypeScript类型定义
│   │   └── index.d.ts
│   ├── utils/                 # 工具函数
│   │   ├── validators.ts      # 自定义验证器
│   │   └── storage.ts         # 本地存储封装
│   ├── views/                 # 页面组件
│   │   ├── home/
│   │   ├── product/
│   │   ├── order/
│   │   ├── chat/
│   │   ├── user/
│   │   └── admin/             # 后台管理（隐藏路由）
│   ├── App.vue
│   └── main.ts
├── .env                       # 环境变量
├── .env.development
├── .env.production
├── package.json
└── vite.config.ts
```

---

## 二、后端技术选型

### 2.1 核心框架
- **Express.js**：轻量灵活，社区生态成熟
- **TypeScript**：strict模式

### 2.2 数据库
- **MySQL 8.0**：阿里云RDS
- **Prisma**：ORM，类型安全，自动生成类型定义

### 2.3 认证与安全
| 技术 | 用途 |
|------|------|
| jsonwebtoken (JWT) | Token生成与验证 |
| bcryptjs | 密码加密 |
| express-rate-limit | 请求频率限制（防刷） |
| helmet | HTTP安全头 |
| cors | 跨域资源共享 |
| express-validator | 请求参数验证 |
| xss-clean | XSS防护 |

### 2.4 WebSocket
- **Socket.io**：
  - 命名空间（namespace）隔离业务
  - 房间（room）管理私聊
  - 心跳检测、断线重连
  - 消息持久化（离线消息）

### 2.5 邮箱服务
- **Nodemailer**：Node.js邮件发送库
- **QQ邮箱SMTP**：发件服务

### 2.6 对象存储
- **阿里云OSS SDK**：
  - 后端移动temp文件到正式目录
  - 获取STS临时凭证（供前端直传）

### 2.7 AI调用
- **阿里云百炼SDK**（@dashscope/sdk）：
  - 调用Kimi2.6模型
  - 简化实现：直接调用，可加简单队列（不加复杂优先级）

> 简化说明：实习项目并发量小，无需复杂的消息队列和优先级机制。审核和发布共用API，直接调用即可。

### 2.8 其他中间件
| 中间件 | 用途 |
|--------|------|
| morgan | HTTP请求日志 |
| dotenv | 环境变量管理 |
| uuid | 生成唯一ID |
| dayjs | 日期处理 |

> 注：文件上传由前端直传OSS，后端只需处理URL，不需要multer

### 2.9 后端目录结构建议

```
backend/
├── src/
│   ├── config/                # 配置文件
│   │   ├── database.ts        # Prisma配置
│   │   ├── oss.ts             # OSS配置
│   │   └── mail.ts            # 邮件配置
│   ├── modules/               # 按业务模块划分
│   │   ├── auth/              # 认证模块
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.routes.ts
│   │   │   └── dto/           # 数据传输对象
│   │   ├── user/              # 用户模块
│   │   ├── product/           # 商品模块
│   │   ├── order/             # 订单模块
│   │   ├── chat/              # 聊天模块（WebSocket）
│   │   ├── ai/                # AI模块
│   │   └── admin/             # 后台管理模块
│   ├── common/                # 公共代码
│   │   ├── decorators/        # 自定义装饰器（@CurrentUser等）
│   │   ├── guards/            # 守卫（AuthGuard、AdminGuard）
│   │   ├── filters/           # 异常过滤器
│   │   └── interceptors/      # 拦截器
│   ├── middlewares/           # 中间件
│   │   ├── auth.middleware.ts
│   │   └── logger.middleware.ts
│   ├── prisma/                # Prisma相关
│   │   └── schema.prisma      # 数据模型
│   ├── utils/                 # 工具函数
│   │   ├── token.ts           # JWT工具
│   │   └── oss.ts             # OSS工具
│   ├── app.ts                 # Express应用入口
│   └── main.ts                # 启动入口
├── .env                       # 环境变量
├── package.json
└── tsconfig.json
```

---

## 三、数据库设计（Prisma Schema）

### 3.1 核心模型

```prisma
// 用户相关
User
  - id, email, username, password, avatar, school, campus
  - phone, role(游客/普通用户/管理员/超级管理员), creditScore
  - createdAt, updatedAt

// 商品相关
Product
  - id, userId, name, description, categoryId, tags
  - images(主图), detailImages(详情图)
  - originalPrice, currentPrice, bargain(支持议价)
  - deliveryType(自提/快递/两者), pickupAddress, pickupTime
  - condition(新旧程度), stock, brand, specs(规格)
  - shippingAddress, validDays, expireTime
  - status(待审核/上架/下架/封禁/审核失败), rejectReason
  - auditCount(审核次数), createdAt, updatedAt

// 分类
Category
  - id, name, parentId(父分类，两级), icon

// 订单
Order
  - id, productId, buyerId, sellerId
  - quantity, price, deliveryType
  - address/pickupInfo
  - status(待支付/待发货/待自提/待收货/待确认自提/已完成/已取消/退货中/已退款)
  - payExpireTime, expressCompany, expressNo
  - createdAt, updatedAt

// 评价
Review
  - id, orderId, reviewerId, reviewedId
  - rating(1-5), content, images
  - status(待审核/通过/违规/已删除), rejectReason
  - type(买家评价/卖家评价), is追评
  - createdAt, updatedAt

// 聊天
Conversation
  - id, user1Id, user2Id, lastMessageId
  - createdAt

Message
  - id, conversationId, senderId
  - type(文本/图片/商品/订单), content
  - readAt, createdAt

// 求购
WantBuy
  - id, userId, name, categoryId, description, tags
  - budget, quantity, images
  - status(未找到/已找到/已关闭/过期)
  - expireTime, createdAt, updatedAt

// 评论（求购贴）
WantBuyComment
  - id, wantBuyId, userId, parentId(回复)
  - content, likes, createdAt

// 收藏
Favorite
  - id, userId, productId, createdAt

// 地址
Address
  - id, userId, name, phone
  - province, city, district, detail
  - isDefault, createdAt

// 通知
Notification
  - id, userId, type, title, content
  - relatedId, relatedType
  - isRead, createdAt

// Banner
Banner
  - id, image, link, sort, enabled
  - startTime, endTime, createdAt

// 举报
Report
  - id, reporterId, targetId, targetType
  - reason, status, result, createdAt
```

---

## 四、API设计规范

### 4.1 RESTful风格
```
GET    /api/products          # 获取商品列表
POST   /api/products          # 发布商品
GET    /api/products/:id      # 商品详情
PUT    /api/products/:id      # 修改商品
DELETE /api/products/:id      # 删除商品

GET    /api/orders            # 订单列表
POST   /api/orders            # 创建订单
GET    /api/orders/:id        # 订单详情
PUT    /api/orders/:id/status # 修改订单状态
```

### 4.2 响应格式
```json
// 成功
{
  "code": 200,
  "message": "success",
  "data": {}
}

// 分页
{
  "code": 200,
  "data": {
    "list": [],
    "total": 100,
    "page": 1,
    "pageSize": 10
  }
}

// 错误
{
  "code": 400,
  "message": "错误描述"
}
```

---

## 五、环境变量配置

### 5.1 前端 .env
```
VITE_API_BASE_URL=http://localhost:3000/api
VITE_WS_URL=ws://localhost:3000
VITE_OSS_REGION=oss-cn-hangzhou
VITE_OSS_BUCKET=your-bucket
```

### 5.2 后端 .env
```
# 数据库
DATABASE_URL=mysql://user:password@host:3306/dbname

# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d

# 阿里云OSS
OSS_ACCESS_KEY_ID=xxx
OSS_ACCESS_KEY_SECRET=xxx
OSS_REGION=oss-cn-hangzhou
OSS_BUCKET=your-bucket

# 阿里云百炼
DASHSCOPE_API_KEY=sk-xxx

# 邮箱
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_USER=your-email@qq.com
SMTP_PASS=your-auth-code

# 支付（模拟）
# PAYMENT_EXPIRE_MINUTES=30
```

---

## 六、技术选型总结

| 模块 | 技术 | 理由 |
|------|------|------|
| 前端框架 | Vue 3 + TypeScript | 你已选，Composition API开发体验好 |
| UI组件 | Element Plus | 你已选，Vue 3版本生态完善 |
| 状态管理 | Pinia | 官方推荐，TypeScript友好 |
| 构建工具 | Vite | 开发启动快，热更新优秀 |
| 后端框架 | Express.js | 你已选，轻量灵活 |
| ORM | Prisma | 类型安全，自动生成类型定义 |
| 数据库 | MySQL 8.0 | 你已选 |
| WebSocket | Socket.io | 自动重连，兼容性好 |
| AI | 阿里云百炼（Kimi2.6） | 你已选，企业级稳定 |
| OSS | 阿里云OSS | 你已选，前端直传+后端管理 |
| 邮箱 | QQ邮箱SMTP | 你已选 |
| 认证 | JWT + bcrypt | 成熟方案 |

---

## 六、实习项目优化建议

> 考虑到这是实习项目，面试官更看重**技术栈深度**和**项目完整度**，而非功能堆砌。以下是针对实习场景的优化建议：

### 6.1 可简化的功能

| 功能 | 原需求 | 简化建议 |
|------|--------|----------|
| 支付模拟 | 30分钟过期、自动取消 | 直接返回成功，不做过期逻辑 |
| 搜索 | Elasticsearch/全文索引 | MySQL LIKE + 基础排序 |
| 信用体系 | 复杂加减分规则 | 简化为基础分+展示 |
| 消息推送 | WebSocket+轮询+浏览器通知 | 仅保留WebSocket实时聊天 |
| 后台管理 | 完整功能 | 核心3-4个页面（商品审核、用户管理） |
| AI并发 | 复杂队列+优先级 | 直接调用，不做队列 |

### 6.2 简历亮点建议

面试时可以让项目加分的点：

1. **WebSocket实战** — 实时聊天、消息已读未读
2. **AI集成** — Kimi大模型调用、Prompt工程
3. **OSS对象存储** — 前端直传、STS临时凭证
4. **TypeScript** — 严格类型、Prisma类型生成
5. **数据库设计** — 合理的关系模型、索引优化

### 6.3 开发优先级

建议开发顺序（按简历价值排序）：

1. **用户系统** — 注册登录、JWT认证（基础）
2. **商品模块** — CRUD、分类筛选、搜索（核心）
3. **订单流程** — 创建、支付、状态流转（业务）
4. **实时聊天** — WebSocket、消息类型（加分项）
5. **AI功能** — 简化版发布/审核/助手（亮点）
6. **后台管理** — 基础管理页面（完整度）

---

## 七、下一步建议

1. **先搭建项目脚手架**
   - 前端：`npm create vite@latest frontend -- --template vue-ts`
   - 后端：`npm init -y`，然后装依赖

2. **数据库设计**
   - 根据上述Prisma Schema设计具体表结构
   - 可以先用MySQL Workbench或Navicat画ER图

3. **开始核心功能开发**
   - 建议顺序：用户注册登录 → 商品发布浏览 → 订单流程 → 聊天

4. **AI功能**
   - Kimi集成是面试亮点，建议保留但简化实现

> 更新时间：2026-05-13